import React from 'react';
import ReactDOM from 'react-dom/client';
import NebulaEdgeApp from './NebulaEdgeApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <NebulaEdgeApp />
  </React.StrictMode>
);
